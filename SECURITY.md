# 安全與隱私

日序是一個離線的個人行程工具，正式版不用帳號、API 金鑰或網路，也不會分析使用行為、同步雲端或自動上傳資料

## 資料範圍

行程、備註、常用事項、完成狀態、偏好設定與提醒紀錄都放在 `%APPDATA%\日序`，SQLite 沒有加密，同一個 Windows 帳號、管理員或已被入侵的系統都可能讀取或修改這些檔案，所以不要用日序保存密碼、API 金鑰或其他機密

錯誤紀錄 `app.log` 可能包含本機路徑與錯誤資訊，資料庫、日誌、截圖、`test-results/` 和 `.private/` 都不要放進公開儲存庫或問題單，移除程式時預設會保留個人資料

## 程式邊界

- Renderer 啟用 sandbox、contextIsolation 並停用 Node 整合，主視窗與提醒小卡各自使用受限的 preload 介面
- IPC 會檢查呼叫視窗、主 frame、完整頁面 URL 與輸入內容，資料庫查詢使用參數化 SQL
- 正式版只載入程式內附頁面，並阻擋外部網路、任意檔案請求、新視窗、導覽、webview 和權限請求
- CSP 禁止遠端腳本、連線、frame、worker 與表單提交，使用者輸入只會當成文字顯示
- 安裝版停用 RunAsNode、NODE_OPTIONS 與 Node 命令列 inspector，啟用 ASAR 完整性檢查，而且只從 ASAR 載入

這些措施能縮小攻擊面，但無法防住已控制本機帳號的惡意程式，也無法保護被整包替換的安裝檔，目前安裝檔沒有程式碼簽章，SHA-256 只能用來確認檔案是否一致

## 更新與回報

日序目前不會自動更新，請從本專案的 Release 手動下載新版，每次發佈都要重新執行 `npm audit`、測試與套件檢查，也要持續更新 Electron 和其他依賴

回報一般錯誤時，只需要提供日序版本、Windows 版本、去識別的重現步驟與測試資料，不要公開尚未修補的攻擊細節或真實行程，安全漏洞請使用 GitHub 的 Security → Report a vulnerability 私下回報
