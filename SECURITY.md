# 安全與隱私

日序是離線的個人行程工具。正式版不需要帳號、API 金鑰或連網權限，不含分析追蹤、雲端同步或自動上傳資料。下載依賴與建置是另外的開發流程，需要網路。

## 資料範圍

行程、備註、常用事項、完成狀態、偏好設定與提醒紀錄保存在使用者的 `%APPDATA%\日序`。SQLite 未加密；同一 Windows 帳號、管理員或已入侵的作業系統可以讀取或修改這些檔案。程式不是密碼管理器，不應以它保存密碼、API 金鑰或需要加密的機密。

錯誤紀錄 `app.log` 可能包含當地路徑與錯誤上下文。資料庫、日誌、截圖和 `test-results/`、`.private/` 不應上傳公開儲存庫或公開問題單。移除程式預設保留個人資料。

## 程式邊界

- Renderer 啟用 sandbox、contextIsolation 並停用 Node 整合。主視窗與提醒小卡分別使用受限 preload 介面。
- IPC 驗證呼叫視窗、主 frame、完整頁面 URL 與輸入內容；資料庫查詢採參數化 SQL。
- 正式版只載入內附頁面，忽略開發伺服器環境變數。阻擋外部網路、任意檔案請求、新視窗、導覽、webview 和權限請求。
- CSP 禁止遠端腳本、連線、frame、worker 與表單提交。使用者文字由 React 以文字顯示，不當作 HTML 執行。
- 安裝版停用 RunAsNode、NODE_OPTIONS 與 Node 命令列 inspector，啟用 ASAR 完整性檢查及只從 ASAR 載入；停用 file 協定額外權限。

這些措施限制應用程式的攻擊面，不防範已控制本機帳號的惡意程式或遭替換的整個安裝包。安裝包目前未做程式碼簽章；SHA-256 只用於檢查檔案是否一致。

## 更新與回報

本程式尚無自動更新，使用者需從可信的專案 Release 手動更新。維護者應在每次發佈執行 `npm audit`、測試、套件檢查，並持續更新 Electron 與依賴；一次沒有已知漏洞不代表永久安全。

回報一般錯誤時，只提供版本、Windows 版本、去識別的重現步驟與測試資料。請勿公開尚未修補漏洞的攻擊細節或真實行程。如果儲存庫已啟用 GitHub Private Vulnerability Reporting，使用 Security → Report a vulnerability；維護者應在首次公開前啟用此功能並確認可用。
